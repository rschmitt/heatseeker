language: rust

rust:
  - stable
  - beta
  - nightly

matrix:
  include:
    - rust: nightly
      env: FEATURES=nightly

script:
   - cargo build --features "$FEATURES"

global:
  env:
    - TRAVIS=1
